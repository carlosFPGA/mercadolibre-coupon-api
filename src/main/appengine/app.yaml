runtime: java
env: flex

runtime_config:  # Optional
  jdk: openjdk8

handlers:
  - url: /.*
    script: this field is required, but ignored

#Define number of instances
#manual_scaling:
#  instances: 1